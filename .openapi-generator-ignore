# Don't generate docs, they don't actually document the methods,
# only the attribute names, which doesn't match the php interface.
docs/**

# The tests are all empty anyway, so let's not bother
test/**

# We don't use this
git_push.sh

.php-cs-fixer.dist.php
.travis.yml
phpunit.xml.dist
